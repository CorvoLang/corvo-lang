# Force changed blocks to disk, update the super block.
prep {
  static.set("version", os.get_env("COREUTILS_VERSION", "0.0.0-dev"))
}
@parsed = args.parse(os.argv(), {})
@o = @parsed["options"]
if (@o.get("help", false)) {
  sys.echo("Usage: sync [OPTION] [FILE]...")
  sys.echo("Force changed blocks to disk, update the super block.")
  sys.exit(0)
}
if (@o.get("version", false)) {
  sys.echo("sync (Corvo coreutils) " + static.get("version"))
  sys.exit(0)
}
sys.sync()
