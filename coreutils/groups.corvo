# Print group names a user is in.
prep {
  static.set("version", os.get_env("COREUTILS_VERSION", "0.0.0-dev"))
}
@parsed = args.parse(os.argv(), {})
@o = @parsed["options"]
if (@o.get("help", false)) {
  sys.echo("Usage: groups [USER]...")
  sys.echo("Print group names a user is in.")
  sys.exit(0)
}
if (@o.get("version", false)) {
  sys.echo("groups (Corvo coreutils) " + static.get("version"))
  sys.exit(0)
}
sys.echo(list.join(os.groups(), " "))
