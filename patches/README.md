# Vendor patches

Place local modifications to vendored crates here as:

```
patches/<crate-name>/<description>.patch
```

Apply after `cargo vendor` (and re-apply after every re-vendor). Document each
patch in `VENDORING.md`.
