FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    libssl3 \
    && rm -rf /var/lib/apt/lists/*

COPY build/out/corvo /usr/local/bin/corvo

RUN chmod +x /usr/local/bin/corvo && corvo --version

ENTRYPOINT ["corvo"]
